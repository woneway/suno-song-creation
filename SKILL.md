---
name: suno-song-creation
description: 用 Suno 进行专业级中文歌曲创作的通用方法论——主题到歌词(视角/意象/韵律)、曲风 profile 驱动的曲式与词法、风格串与参数配置、生成与迭代修正、去AI味、发布合规。适用于任何曲风(民谣/流行/摇滚/R&B/说唱/中国风/电子/爵士/蓝调/bossa/city-pop…)。当用户想创作新歌、写歌词、调整已生成歌曲的曲风/情绪/音色/节奏、或准备发布(封面/介绍/合规)时使用。
---

# Suno 中文歌曲创作 Skill(通用)

## 何时用(场景 → 入口)

| 用户场景 | 从哪进 |
|---------|--------|
| 提出新歌主题("想写一首关于……的歌") | 创作流程第 1 步 |
| 生成结果**不像目标曲风** | genre-profiles 对应 profile:身份要素逐条检查 → 常见跑偏 → 词面;禁止只改风格串 |
| 生成结果情绪/音色/节奏/咬字不对 | 分层控制模型,定位后走第 7 步迭代 |
| 要发布(专辑名、介绍、封面、合规) | 创作流程第 8 步 |
| 问"怎么写风格串/某标签有没有用" | references/suno-spec.md + proven-templates.md |

## 核心认知(先读)

1. **Suno 是"概率执行者",不是 DAW**。prompt 决定抽卡分布,不是签合同。策略永远是:锁死方向(词+标签+Exclude)→ 多抽 → 按预定义的"成败句"挑 → 局部修(Replace Section),不整首重抽。
2. **词是唯一完全受控的层**,也是版权归属的关键——著作权保护要求人类独创性投入,自写歌词是最有力的证据(不存在公开的量化百分比门槛)。词上多花时间,生成上少浪费额度。
3. **曲风 profile 先行,主流程无全局默认**。本文件只提供体裁无关的方法论骨架;曲式、句长结构、韵法密度、hook 机制、情绪弧线形状、收尾装置**都没有跨曲风的默认值**——genre-profiles.md 的对应 profile 是这些参数的**唯一默认值来源**(不是对某个"通用默认"的增量修正)。profile 缺某字段时,按其字段结构现场调研补齐并回写,**禁止从其他曲风 profile 或本仓案例借值**。跳过 profile 直接写词 = 用上一首歌的模具写新歌(历史教训:民谣模具套爵士,产出"换皮不换骨"的流行歌)。
4. **每首歌是新的设计,不是上一首的变体**。写词前必须做装置指纹查重(第 4 步)——曲风换了不等于装置换了,《归燕》与《长椅宽出一半》v1 共用"揭底低语+哼唱收尾+镜像副歌"整套装置,就是同一首歌的两个皮(用户一听即打回)。
5. **情绪/曲风问题优先查配置与词的结构,再查词面**。历史教训:Exclude 里的 `upbeat` 压死过欢快段落;从民谣沿用的防欢快池杀死过爵士的 swing;规整对称句把爵士旋律拉回流行。用户连续两次反馈同一问题 = 停止改词面,查结构性原因。
6. **用户的耳朵是唯一验收标准**。我听不到音频;每轮生成后引导用户用文字描述"哪里不对"(给选项:哪一段/什么方向),再定位到对应控制层。
7. **可唱性是基线,不是可选目标**(2026-07-06 用户裁定:"人能跟着唱,是基线,所有的歌曲都需要满足这个条件")。基线五条(核心可跟段/节拍锚/大众音域/一字一音/低词负荷)与各曲风实现路径见 **references/singability.md**,第 2 步与 profile 一起载入;低跟唱潜力曲风(爵士/bossa/技术流说唱…)的上限在**选型期**告知用户并确认,不许生成后才暴露。

## 分层控制模型(哪里不对改哪层)

| 症状 | 控制层 | 动作 |
|------|--------|------|
| **不像目标曲风** | profile | 对照 profile 身份要素逐条检查哪条丢了 → 查常见跑偏是否命中 → 改词的结构(句长/曲式/留位),最后才改风格串 |
| 歌词看不懂/绕口 | 词 | 意象一步能懂(profile 可放宽两步);隐喻藏在关系里不藏在生造搭配里 |
| 咬字/发音错 | 词 | 多音字换字(查 music-theory 1.6 高危清单 + proven-templates「发音踩坑」) |
| 唱法/力度不对(太炸或太收) | 风格串+Exclude | 按 profile 人声字段写;"克制三件套"(restrained/never loud/pull back)只适用于克制型歌曲,不是通用药 |
| 唱得太平/没起伏 | 段落标签 | 每段写 `[段落: 配器+情绪]`;弧线形状按 profile(爬升/循环/平缓微澜/克制反转) |
| 情绪跑偏(太悲/太欢/太干) | Exclude | 每首单独设计,防"这首歌最可能的跑偏方向"(查 profile 常见跑偏),不抄池子(否定指令写在标签里无效) |
| 节奏赶/机器感 | 风格串 | **按律动体系分流**:自由律动体裁(民谣/爵士/bossa/R&B)用 `natural loose timing, breathes between phrases` / behind-the-beat;网格律动体裁(电子/说唱/朋克)禁用 loose timing 类词(病根在鼓型/音色,不在拖拍);BPM 按 profile 区间校 |
| 音色不对/不稳 | Persona/Voice | 好音色从歌曲提取存 Voice 复用(2026-06 实测可用);唱法被带偏就降 Audio Influence 或摘掉 |
| 旋律平庸/旋律像别的曲风 | 词的节奏型 | 行长结构是旋律结构的杠杆:要规整给对称句,要摇曳给错落句(方向按 profile);hook 机制按 profile |
| 两段没对比 | 风格串结构 | 同一声音世界内做密度对比(分开生成再拼会割裂,已验证否决) |
| 没法跟着唱/听几遍也记不住 | 基线层 | 过 singability.md 基线五条定位缺哪条:无核心可跟段=词端杠杆(短句/一字一音/逐字重复);节拍锚消失=配置端;音域飙=筛选端换版本;基线与曲风上限冲突=回选型期与用户对齐 |
| 环境音没出 | 后期 | `[Sound Effect: ...]` 抽卡两轮不中就 GarageBand 叠音效,别恋战 |

## 平台机制事实(与曲风审美无关,勿混入审美处方)

- **同文本行 → 相似旋律**:想让两处唱同一旋律,给逐字相同的文本。要不要重复、重复多密,由 profile 决定。
- **"相似"不等于"逐音一致"——段落级旋律严格重复不会自动涌现**(自回归生成的已知局限,Dai et al. 2022:每次"副歌"是重新采样而非引用前段)。词端逐字重复+同标签只是拉高概率;跟唱级一致性最终靠筛选端把关(singability.md 第五节)。
- **AI 按行谱曲,行长结构决定旋律结构**:相邻行字数对称 → 旋律规整重复;错落 → 错落乐句。方向是曲风选择,不是优劣。
- 标签是**概率性引导非硬约束**;`[段落名: 配器+情绪]` 参数化标签遵循度最高;`[End]` 防拖尾;拼错的标签会被唱出来。
- **V-C 标签遵循度高于其他曲式标签,但这是概率差异,不是体裁禁令**。注意编号的语义:`[Verse 1]/[Verse 2]` 引导"各段**不同**旋律"——想让多个段落**同旋律**(如 AABA 的三个 A 段),用**完全相同的标签文本(不编号)+ 同构的行长骨架 + 相同的段尾 refrain 行**三重信号(见 genre-profiles 爵士节)。
- 否定指令写在**段落标签**里无效;**Styles 栏**否定句 V4.5+ 起生效但可靠性低于 Exclude 栏——防跑偏主用 Exclude,Styles 否定句仅作补充(口径同 suno-spec §3)。
- 分开生成两段再拼接会割裂;对比在同一声音世界内做。

## 创作流程

### 第 1 步:主题访谈

问出:给谁写/什么故事/**真实细节**(细节是歌的命)/想要的情绪走向/**谁唱**(人声性别与声部意向——Vocal Gender 滑块与人声描述的来源;KTV 自唱场景按用户本人的声部与音域舒适区;**用户未答时不猜默认 male**——按题材暂定[情歌按演唱者、群像/合唱/文旅按内容]并 flag,生成前与用户确认)/**曲风意向**("想要什么感觉""像谁的哪类歌"→用六维拆解法转译,见 production-advanced 第五节;提示词禁写艺人名)/**跟唱档位**(基线=核心段可跟,是所有歌的前提;K歌向=全曲可跟;合唱传播向=参与工程全开——档位定义见 singability.md 第三节;用户给出多个场景需求时按最高档讨论)。

### 第 2 步:曲风确立与 profile 载入

- 确定曲风(含子曲风,如爵士分 swing/ballad)→ 载入 **references/genre-profiles.md** 对应 profile。
- **同时载入 references/singability.md(可唱性基线层,全曲风必载)**:先过其"曲风跟唱潜力表"——用户档位需求超出该曲风上限时,此刻就与用户对齐(换曲风或降档),禁止静默牺牲基线或曲风身份。
- profile 的**身份要素 = 本歌不可妥协的约束**,生成与迭代的资源优先保它;词法字段覆写后续所有默认值(与基线五条冲突时,冲突暴露给用户取舍)。
- 库里没有的曲风:按 profile 字段结构现场调研补一份,入库复用。

### 第 3 步:视角、意象系统与韵律设计

- **先定叙事视角**(谁在对谁说、隔着多远)——视角先于文笔;各曲风默认姿态查 profile。人称默认不漂移,漂移必须是显式装置。
- 一首歌**一个中心**(题眼可被听者复述)。意象经营两条路线按题材选:群铺陈(中国风)vs 单意象纵深(短歌/叙事/爵士 conceit);说唱可高频换喻。
- 隐喻分寸:**每个意象单看一步能懂**(爵士/说唱/诗性摇滚可放宽到两步);用户指出任何一句"看不明白"即重写该句——押韵不得牺牲口语自然度(凑韵三俗禁令:倒装/生僻/人称漂移)。
- 韵辙:按情绪选辙(查 music-theory 1.1 十三辙表——中文演唱物理,全曲风通用);**押韵密度与换韵策略按 profile**(一韵到底是民谣/流行的一个选项,不是默认;说唱要求换韵;爵士稀疏韵+头韵补位)。

### 第 4 步:结构设计与装置查重(写词前做,写完再查会舍不得改)

- **曲式按 profile**(V-C / AABA / 分节歌 / 12-bar AAB / build-drop);**器乐段留位按 profile**(爵士/蓝调/摇滚的词必须给 solo 留结构位)。
- 从 proven-templates**装置库**按题材选装置(进入/推进/收尾/hook 四类菜单),**禁止默认沿用上一首歌的装置组合**。
- **装置指纹查重(硬门禁)**:按 9 维指纹(视角/进入方式/推进装置/意象域与路线/韵辙与换韵/曲式与 hook 机制/题眼句型/弧线形状/收尾装置)与仓库最近 3–5 首**逐首两两比对**,任一首重合 ≥3 维即触发 → **强制换掉 ≥2 维**(优先换:收尾装置、题眼句型、进入方式)。**判重口径**:①视角=人称/对象/时态三要素 ≥2 同才算重(例外:泛第一人称+现在时是全曲风高基率组合,仅此两项同不计,需对象也同);②意象=域同才算重,经营路线同不算;③题眼句型=句型分类同即算(参考分类:意象命名/宣言/否定/问句/祈使/承诺/话语反转/反讽陈述/白描陈述——开放表,新句型自增);④**豁免"曲风必然",不豁免"自由度内趋同"**(每项须记录属哪类):(a)**无选择余地的强制字段**(说唱必然多押、爵士必然 swing、V-C 曲式必然有重复副歌)整维豁免;(b)**profile 内仍有多个合法选项的字段**(弧线形状可平缓微澜/可克制反转、hook 机制可镜像/变奏/逐字重复、进入方式、意象路线;**视曲风而定**——仅当该 profile 对该字段确留 ≥2 合法选项才归 (b),单值锁死者[如说唱必然多押]归 (a))**不整维豁免**——两首同曲风歌选了相同选项(都用克制反转顶点、都用镜像副歌)照常计重(否则"整维豁免弧线/hook"会放过听感很像的两首——民谣演练实证)。跨曲风比对时两 profile 各自(a)类同名字段撞车(如 bossa 与爵士同为平缓微澜)豁免;⑤韵辙=辙的使用**序列/分布策略**同才算重(单辙相同不算——十三辙基数小,同辙是高基率事件;判例:两首都"一韵到底"但辙不同=策略同、材料异,不计重;都"主歌一辙、副歌换另一辙"且换韵位置同=计重);⑥换曲风不抵扣查重;⑦用户明确要系列感时显式记录豁免。**判模具的主次——框架维 vs 声响装置维**:视角/进入方式/题眼句型是"框架维"(由题材用途决定,同用途歌[邀请体验/合唱告别]天然趋同);意象域/韵辙/曲式-hook/推进装置/弧线/收尾是"声响装置维"。**以声响装置维为主判模具**:框架维趋同 3 维但声响装置维全异 ≠ 模具(同用途歌的正常相似,非"一听即像");声响装置维重合才是真警报(长椅 v1≈归燕正是收尾/hook/弧线等装置维 6–7 维重合)。指纹存档与判例见 proven-templates 第六节。
- **词面回声/手癖查重(9 维之外的独立层,与装置指纹正交,同为硬门禁)**:9 维查结构装置,查不到**作者手癖的词面复制**——施动结构(「风把 X 吹得 Y」)、否定句式(「没人 X」)、叠词(「V 来 V 去」)、串级近似的 hook 行。这些逐字/句式级回声是"换曲风≠换装置"在**词面层**的复发路径:长椅 v1 被打回、本轮民谣演练初稿撞正午,都是 9 维全过却仍"一听即像"。成稿对最近 3–5 首扫一遍手癖清单(proven-templates 六),命中即改。**通过 9 维 ≠ 与旧歌无词面回声。**
- 情绪弧线**形状**按 profile:爬升型/循环型/平缓微澜型/克制反转型——"要有被设计的能量曲线"通用,形状不通用。

### 第 5 步:写词(带段落标签)

- 段落标签格式:`[段落名: 配器 + 情绪]`——只写这两件事,短而明确执行率最高。
- 词曲咬合(机制通用,方向按 profile):标点是停顿指令,长句用标点拆气口;一行 = 一口气 = 一个乐句;行尾虚词默认避免拖长(说唱/spoken 段放宽,bossa 语气词收尾可作装置);**行长结构按 profile 方向**(流行/民谣求对称,爵士/说唱求错落)。
- hook 写法按 profile 机制(流行=逐字重复轰炸;爵士=title 回归 3 次、末次微变;蓝调=B 句反转;EDM=短语循环)。
- **核心可跟段执行 singability.md 词端杠杆**(短句一字一音/开口元音长音/逐字重复/参与位);主歌按跟唱档位与 profile 放宽。
- 交稿前过 lyrics-advanced 第四节 **15 项 Review Checklist**(音韵层/内容层通用;工程层按 profile 参数执行,含第 13 项装置复检、第 14 项可唱性基线核检与第 15 项词面回声查重)。

### 第 6 步:配置生成

- 风格串按六维组装公式(production-advanced 第五节)+ **profile 的 Suno 要点**;proven-templates 的案例模板**只在同体裁时参考**,跨体裁禁止直接套用。
- Exclude **每首单独设计**:防的是"这首歌最可能的跑偏方向"(查 profile 常见跑偏),3–5 项,禁止不经分析照抄上一首的池子——同曲风方向趋同、逐项确认后重词不算复制(口径见 proven-templates 二;历史教训:民谣防欢池杀死爵士 swing)。
- 滑块按 proven-templates「More Options 配置」起步;粘贴后 30 秒自查(标签唯一性、无标题文字混入、Exclude 完整)。平台规格查 suno-spec.md。

### 第 7 步:迭代

- 生成前**为本歌预定义 1–2 个成败句**(按这首歌的设计定,勿沿用上一首的成败句型);2 条 × 3–5 轮起步,难收敛加到 5–8 轮。
- 挑版本:先按成败句,再过 proven-templates「生成结果听审清单」(artifact 排查与**可唱性基线组**为一票否决 + 音乐性;"顶点在预期位置"的预期由 profile 定义)。
- **「不像曲风」反馈**:走分层控制模型第一行(identity → 跑偏 → 词的结构),不要陷在风格串里加词。
- Remix vs Edit:90 分版本走 Edit(Replace Section/Extend/Studio);Remix→Cover 只用于"旋律好但演绎方向错"(Cover 是再生成,歌会变)。**唱法/旋律/曲风是全局属性,错了要整体重抽,Replace Section 救不了。**
- 同一问题用户反馈 2 次未解决 → 停止改词面,按分层控制模型做根因排查,向用户报告假设再动手。

### 第 8 步:发布

- 后期与音频 QC:轻量走 GarageBand(叠环境音/呼吸声);发行级走 production-advanced 第四节完整链(-14 LUFS / -1 dBTP)。
- 封面 ≥1440×1440(`sips -z 1440 1440 封面.png --out 封面_1440.png`),无 logo/水印。
- 歌曲介绍:用歌的意象写,揭示节奏与歌一致。
- 合规清单(AI 标注/商用权/发行通道/声音权)以 proven-templates「发布合规」为**唯一事实源**。
- **每首歌建文件夹** `<歌名>/`,内置 `配置.md`(全量歌词/风格/参数/踩坑/胜出版本链接,**并含「装置指纹」小节**供后续查重)、成品音频、封面。

## 参考文件

- **references/genre-profiles.md** — 曲风 profile 库(12 曲风;身份要素/曲式/词法/弧线/跑偏/Suno 要点)。**第 2 步必读,词法冲突时 profile 胜**
- **references/singability.md** — 可唱性基线层(基线五条/跟唱档位与曲风实现路径/词端配置端杠杆/筛选端硬判据)。**第 2 步与 profile 一起必载;基线与 profile 冲突时暴露给用户取舍**
- **references/proven-templates.md** — 案例库与操作手册:本仓案例风格串(标注体裁)、装置库、装置指纹存档、Exclude/滑块、标签写法、去AI味、听审清单、发布合规
- **references/music-theory.md** — 音乐基础知识:十三辙、倒字、曲式、BPM/拍号/和声、配器、人声术语(写词与写风格串时查)
- **references/lyrics-advanced.md** — 歌词进阶:大师技法、押韵进阶、词曲咬合机制、交稿 15 项清单(写词与审词时查)
- **references/production-advanced.md** — 制作进阶:旋律文字化四通道、编曲 build 曲线、弦乐写法、发布级后期链、参考曲六维拆解
- **references/suno-spec.md** — Suno 平台规格:字段上限、标签清单、滑块机制、编辑功能门槛、中文已知问题(配置时查;平台迭代快,以标注日期为准)

## 时效声明

Suno 版本、订阅权益、平台政策迭代极快;所有"实测/规格"类结论以文内标注日期为准,使用时若发现行为不符,以当场实测为准并回写更新对应文件。
